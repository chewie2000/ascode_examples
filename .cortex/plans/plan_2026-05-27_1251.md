---
created: 2026-05-27T12:51:19.676Z
session: 73db3ff6-7f21-4244-b1cc-af393d0bd493
working_directory: /Users/mark.oldfield/ascode_examples
---

## Context\n\n**Data model (source):** Sales model (`0ce5d87d-0828-4339-b7ad-6e8c599fc626`), main FACT_SALES element ID: `3hSctKvD1A`\n\nThe workbook will source from the data model, which already has:\n- FACT_SALES columns: Transaction ID, Sale Date, Store ID, Product ID, Customer ID, Quantity, Amount, Appointment ID, Revenue (calc)\n- Relationships to DIM_STORE (Store Name, City, State, Region) and DIM_WATCHES (Brand, Watch Name, Price, Rating)\n- Metric: Total Revenue = Sum(Revenue)\n\n**Schema version:** 1 (from existing workbook)\n**Folder:** `83901b11-e356-4d17-b295-43e3c956b9f3` (Workbook-as-code)\n\n## Implementation Steps\n\n1. **Compose workbook spec** with:\n   - **Page 1: \"Sales Overview\"**\n     - A **table** element sourcing the data model's FACT_SALES element (shows Sales Date, Store ID, Product ID, Quantity, Amount, Revenue)\n     - A **KPI** for Total Revenue (`Sum([Sales Data/Revenue])`)\n     - A **bar chart** — Revenue by Region (sourcing the table element)\n     - A **line chart** — Revenue over time by month (sourcing the table element)\n\n2. **Validate** the spec with `./scripts/validate-spec.sh`\n\n3. **POST** to `/v2/workbooks/spec` with `Accept: application/json`\n\n4. **Save** the spec to `models-as-code/` (or report the URL)\n\n## Verification\n\n- HTTP 200 on POST with `workbookId` returned\n- Open the workbook URL in browser to confirm it renders\n\n## Critical Files\n\n- `/Users/mark.oldfield/.claude/skills/sigma-workbooks/reference/specification/formulas.md` — Formula qualification rules\n- `/Users/mark.oldfield/.claude/skills/sigma-workbooks/reference/specification/charts.md` — Chart element shapes\n- `/Users/mark.oldfield/.claude/skills/sigma-workbooks/reference/specification/kpis.md` — KPI element shape\n- `/Users/mark.oldfield/.claude/skills/sigma-workbooks/reference/workflows/crud.md` — POST/GET/PUT mechanics\n- `/Users/mark.oldfield/.claude/skills/sigma-workbooks/scripts/validate-spec.sh` — Pre-submit validator"